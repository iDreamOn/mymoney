<% provide(:title, "Categories") %>
<p id="notice"><%= notice %></p>

<h1>Listing Categories</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %><% if category.cc_payment? %><sup><a href="#fn1" id="ref1"> *</a></sup><% end %></td>
        <td><%= category.description %></td>
        <td><%= link_to 'Show', category %></td>
        <td><%= link_to 'Edit', edit_category_path(category) %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_category_path %>

<hr></hr>

<p>
<sup id="fn1">*: Credit Card Payments are not counted in spendings since you are actually paying back an expense you did hopefully not to long ago. <a href="#ref1" title="Jump back to footnote 1 in the text.">↩</a></sup><br>
</p>
